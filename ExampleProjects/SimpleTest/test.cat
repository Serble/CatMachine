
; this is the Fibonacci sequence
mov r0, 1
mov r1, 1
loop:
mov r2, r0
add r0, r1
mov r1, r2

umul r2, 10
shl r2, 4
shr r2, 4

cmp r2, 5
cmp 6, 9
je loop

jmp loop




mov r2, 0
start:
int 0x85
cmp r0, r2
jul start
mov r1, r0
int 0x90
add r2, 1000
jmp start


int 0x83
