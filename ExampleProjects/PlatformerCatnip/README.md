# Platformer
This platformer game is written entirely in Catnip. It features velocity, level loading, death, and jumping.

The level data is stored in manually modified binary files in `data/levels` where the value of each byte
represents the tile type.

The controls are WASD.
